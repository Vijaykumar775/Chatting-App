import { Menu } from '@mui/material'
import React from 'react'

const Filemenu = ({anchorE1}) => {
  return <Menu open={false} anchorEl={anchorE1}>
  <div style={{
    width:"10rem"
  }}>
    sad
  </div>
  </Menu>
}

export default Filemenu
